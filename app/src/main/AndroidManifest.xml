<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_ema"
        android:label="@string/ema_strings_app_name"
        android:roundIcon="@mipmap/ic_launcher_ema"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicSchool"
        tools:targetApi="31">
        <activity
            android:name=".activities.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.DashboardActivity" />
        <activity android:name=".activities.PerformanceActivity" />
        <activity android:name=".activities.CollaborationActivity" />
        <activity android:name=".activities.UserProfileActivity" />
        <activity
            android:name=".activities.EditProfileActivity"
            android:parentActivityName=".activities.DashboardActivity" />
    </application>

</manifest>